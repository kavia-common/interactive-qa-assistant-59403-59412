# Frontend environment variables
# IMPORTANT: Do not store any API keys here. The frontend must call a backend proxy.
# Set this to your backend base URL (without trailing slash). Example:
# REACT_APP_API_URL=https://your-backend.example.com
# If unset, the app will default to same-origin and call /answer on the same host.
REACT_APP_API_URL=
